{
  "name": "@nelie-taylor/agent-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=18"
  }
}
